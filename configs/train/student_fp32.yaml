# V3 历史单阶段配置；V4 统一使用 v4_量产训练.yaml 阶段运行器
status: historical_v3_superseded
seed: 20260804
device: auto
patch_sizes: [256, 384]
late_patch_size: 512
batch_size: 8
steps: 400000
learning_rate: 0.0002
weight_decay: 0.0001
gradient_clip: 1.0
warmup_steps: 5000
scheduler: cosine
loss_weights: {raw: 0.55, tone: 0.30, gradient: 0.15}
